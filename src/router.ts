import {
  createRouter,
  createWebHistory,
  type RouteRecordRaw,
} from "vue-router";

const routes: RouteRecordRaw[] = [
  // TODO
];

export const router = createRouter({
  history: createWebHistory(),
  routes,
});
